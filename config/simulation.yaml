# Rendering config — shared by both collection tasks and both eval scripts.
# Reach's env/expert live under collect.tasks.reach in collect.yaml (the
# single source both collection and eval read from).

renderer:
  height: 480
  width: 640
